import { CheckmailDirective } from './checkmail.directive';

describe('CheckmailDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckmailDirective();
    expect(directive).toBeTruthy();
  });
});
